spring:
  main:
    banner-mode: off

  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql:///mbulletin?serverTimezone=UTC&useSSL=false
    username: root
    password: root

  redis:
    host: localhost
    port: 6379
